# Ayaselva Content Repo

This repository is the canonical editable content source for:

- site navigation/settings (`site.yaml`)
- pages (`pages/*.yaml`)
- blog posts (`posts/*.md`)

The Astro app syncs these files into `astro/app/src/data/cms` and `astro/app/src/content/post` before build/dev.
